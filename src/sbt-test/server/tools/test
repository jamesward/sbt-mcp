# Compile and seed the index while the root project is active, then query the
# server from the forked Scala 3.9 zio-http-mcp 0.8.2 harness.
> compile
> mcpStatus
> mcpPrepareTools
> harness/run

# Verify the in-process command primitive that sbt-task uses.
> mcpInJvmCheck

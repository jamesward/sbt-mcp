# The single process-global MCP server must report status once, not once per
# aggregated subproject.
> mcpStatus
> checkStatusNoAggregate

# Compile the aggregate and seed its Scala 3.9 symbol index, then query it from
# the forked zio-http-mcp 0.8.2 harness.
> compile
> prepareMultiModuleSymbols
> harness/run

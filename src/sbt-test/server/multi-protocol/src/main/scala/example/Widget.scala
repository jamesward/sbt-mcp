package example

// Minimal source so `compile` has something to build; the test only exercises MCP
// tool listing over multiple protocols, not the symbol tools.
final case class Widget(label: String)

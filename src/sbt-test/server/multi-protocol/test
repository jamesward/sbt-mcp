# Validate that our MCP server lists ALL tools — built-ins plus proxied upstream tools —
# under EVERY supported MCP protocol version (modern 2026-07-28 stateless and the legacy
# handshake revisions). Guards against a proxied-tool merge regressing on one protocol path.
> compile
> mcpCheckProtocols
